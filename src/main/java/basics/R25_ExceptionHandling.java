package basics;

public class R25_ExceptionHandling {
    /*
    //Checked
    -compiler warns you to handle it.
    -extends Exception
    - caller has to handle it incase of multi calls (a->b-<ce)
    - incase caller dont want to handle, caller itself should add in method signature

    //Unchecked
    // happens only during runtime
    // extends run time exception
    //
    */

}
